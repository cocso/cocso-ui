import * as TabPrimitive from '@radix-ui/react-tabs';

export const Tab = Object.assign(TabPrimitive.Root, {
  List: TabPrimitive.List,
  Trigger: TabPrimitive.Trigger,
  Content: TabPrimitive.Content,
});
