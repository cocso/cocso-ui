kind: Tokens
metadata:
  id: typo
  name: Typography
  description: 타이포그래피 토큰
data:
  collection: typo
  tokens:
    $typo.font-family.sans:
      values: '-apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif'
      description: 산세리프 폰트 패밀리
    $typo.font-family.serif:
      values: 'Georgia, Cambria, "Times New Roman", Times, serif'
      description: 세리프 폰트 패밀리
    $typo.font-family.mono:
      values: 'ui-monospace, SFMono-Regular, "SF Mono", Monaco, Consolas, "Liberation Mono", "Courier New", monospace'
      description: 모노스페이스 폰트 패밀리
    $typo.font-size.xs:
      values: '12px'
      description: 매우 작은 폰트 크기
    $typo.font-size.sm:
      values: '14px'
      description: 작은 폰트 크기
    $typo.font-size.base:
      values: '16px'
      description: 기본 폰트 크기
    $typo.font-size.lg:
      values: '18px'
      description: 큰 폰트 크기
    $typo.font-size.xl:
      values: '20px'
      description: 매우 큰 폰트 크기
    $typo.font-size.2xl:
      values: '24px'
      description: 2배 큰 폰트 크기
    $typo.font-size.3xl:
      values: '30px'
      description: 3배 큰 폰트 크기
    $typo.font-size.4xl:
      values: '36px'
      description: 4배 큰 폰트 크기
    $typo.font-weight.light:
      values: 300
      description: 얇은 굵기
    $typo.font-weight.normal:
      values: 400
      description: 보통 굵기
    $typo.font-weight.medium:
      values: 500
      description: 중간 굵기
    $typo.font-weight.semibold:
      values: 600
      description: 반굵은 굵기
    $typo.font-weight.bold:
      values: 700
      description: 굵은 굵기
    $typo.line-height.tight:
      values: 1.25
      description: 좁은 줄 간격
    $typo.line-height.normal:
      values: 1.5
      description: 보통 줄 간격
    $typo.line-height.relaxed:
      values: 1.75
      description: 넓은 줄 간격
