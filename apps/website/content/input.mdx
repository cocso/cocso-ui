---
title: Input
description: 사용자 입력을 수집하기 위한 텍스트 필드입니다.
---

<Section>
  <Section.Title>개요</Section.Title>
  <Section.Text>
    Input 컴포넌트는 한 줄 텍스트 필드입니다. 레이블, 설명, 에러 메시지, 다양한 크기를 지원합니다. 폼, 검색 필드, 자유 형식의 텍스트 입력이 필요한 모든 시나리오에 사용하세요.
  </Section.Text>
</Section>

<Section>
  <Section.Title>기본</Section.Title>
  <Section.Text>
    레이블과 플레이스홀더가 있는 기본 입력 필드입니다.
  </Section.Text>
  <ComponentExample name="input-default">
    ```tsx
    import { Input } from "@cocso-ui/react";

    export default function InputDefault() {
      return (
        <div className="flex w-full max-w-sm flex-col gap-4 p-4">
          <Input label="Email" placeholder="Enter your email" />
        </div>
      );
    }
    ```
  </ComponentExample>
</Section>

<Section>
  <Section.Title>크기</Section.Title>
  <Section.Text>
    주변 콘텐츠 밀도에 맞게 네 가지 크기를 사용할 수 있습니다.
  </Section.Text>
  <ComponentExample name="input-sizes">
    ```tsx
    import { Input } from "@cocso-ui/react";

    export default function InputSizes() {
      return (
        <div className="flex w-full max-w-sm flex-col gap-4 p-4">
          <Input label="Large" placeholder="Large input" size="large" />
          <Input label="Medium" placeholder="Medium input" size="medium" />
          <Input label="Small" placeholder="Small input" size="small" />
          <Input label="X-Small" placeholder="X-Small input" size="x-small" />
        </div>
      );
    }
    ```
  </ComponentExample>
</Section>

<Section>
  <Section.Title>상태</Section.Title>
  <Section.Text>
    도움말 텍스트에는 <code>description</code>, 유효성 검사 메시지에는 <code>error</code>, 상호작용 방지에는 <code>disabled</code>를 사용합니다.
  </Section.Text>
  <ComponentExample name="input-states">
    ```tsx
    import { Input } from "@cocso-ui/react";

    export default function InputStates() {
      return (
        <div className="flex w-full max-w-sm flex-col gap-4 p-4">
          <Input label="With description" description="We'll never share your email." placeholder="Email" />
          <Input error="This field is required." label="With error" placeholder="Required field" />
          <Input disabled label="Disabled" placeholder="Cannot edit" />
        </div>
      );
    }
    ```
  </ComponentExample>
</Section>

<Section>
  <Section.Title>Stretch</Section.Title>
  <Section.Text>
    입력 필드가 컨테이너의 전체 너비를 채우도록 하려면 <code>stretch</code>를 설정합니다.
  </Section.Text>
</Section>

<Section>
  <Section.Title>API 레퍼런스</Section.Title>
  <PropsTable
    data={[
      {
        name: "label",
        type: "string",
        description: "입력 필드 위에 표시되는 레이블 텍스트입니다.",
      },
      {
        name: "size",
        type: '"large" | "medium" | "small" | "x-small"',
        default: '"medium"',
        description: "크기를 지정합니다.",
      },
      {
        name: "disabled",
        type: "boolean",
        default: "false",
        description: "비활성화 여부를 지정합니다.",
      },
      {
        name: "error",
        type: "boolean | string",
        default: "false",
        description: "문자열인 경우 입력 필드 아래에 에러 메시지로 표시합니다. true인 경우 에러 스타일만 적용합니다.",
      },
      {
        name: "description",
        type: "string",
        description: "입력 필드 아래에 표시되는 도움말 텍스트입니다.",
      },
      {
        name: "stretch",
        type: "boolean",
        default: "false",
        description: "true일 때 입력 필드가 컨테이너 너비에 맞게 확장합니다.",
      },
    ]}
  />
</Section>

<Section>
  <PageNavigation />
</Section>
