---
"@cocso-ui/react": minor
---

`Card` gains `variant="glass"`: `surface-glass` over a 16px `backdrop-filter` blur with a `border-glass` edge. The same variant reaches SwiftUI and Compose from the recipe.
