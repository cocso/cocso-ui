---
"@cocso-ui/recipe": minor
"@cocso-ui/react": patch
---

`radio` recipe: a selected radio keeps the page's fill and takes `interactive-primary` on its ring — what the web has always drawn through `[data-checked]`. The recipe had filled the whole circle with the primary, a value the web never read and the mobile views did, so a selected radio was a solid disc there. No visual change on the web.
