---
"@cocso-ui/recipe": minor
---

`card` and `button` recipes: `glass` variant (`surface-glass`, `border-glass`; the button presses to `surface-glass-active`). `SEMANTIC_TO_PRIMITIVE` maps the glass tokens so Figma no longer previews them as magenta.
