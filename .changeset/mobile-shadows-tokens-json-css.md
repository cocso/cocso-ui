---
"@cocso-ui/css": minor
---

Publish `tokens.json`: every token resolved per mode, with each brand's override inline and what was skipped and why — the same resolution the Swift and Kotlin artifacts come from, for consumers that are programs. `cocso/mobile` parsed `CocsoTokens.swift` with regular expressions; it can read this instead.
